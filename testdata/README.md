# Test Fixtures

Integration tests create disposable repositories, certificate archives, and secret
canaries under the test process temporary directory. No live credential, internal
database export, or production certificate is stored here.
